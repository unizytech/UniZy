# FastAPI and ASGI server
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Pydantic for data validation (flexible for dependency resolution)
pydantic>=2.11.7,<3.0.0
pydantic-settings>=2.6.1

# Google Gemini SDK
google-genai==2.7.0
google-cloud-storage>=2.16.0   # GCS uploads for Vertex AI mode large-audio path

# Additional dependencies
python-multipart==0.0.20  # For file uploads
python-dotenv==1.0.1      # For environment variables
httpx==0.28.1              # HTTP client for async requests
python-dateutil>=2.8.0     # For date/age calculations (relativedelta)
Jinja2>=3.1.0              # Consult-letter rendering for radiology templates

# Live Recording dependencies
sse-starlette>=2.2.1       # Server-Sent Events for real-time updates
supabase>=2.23.0           # Supabase Python SDK for database operations (compatible with httpx>=0.28)

# Medicine matching
rapidfuzz>=3.6.0           # Fuzzy string matching for medicine name matching

# Audio processing
pydub>=0.25.0              # Audio format conversion and manipulation
librosa>=0.10.0            # Audio quality analysis (SNR, RMS, VAD)
soundfile>=0.12.0          # Audio file I/O for librosa

# Excel export (POC metrics)
openpyxl>=3.1.0            # XLSX workbook generation for POC metrics admin export

# Caching
cachetools>=5.3.0          # In-memory TTL cache for reducing DB queries

# Q&A Engine - Embeddings
cohere>=5.0.0              # Cohere embeddings API (healthcare fine-tuned)
pgvector>=0.3.0            # PostgreSQL vector extension client
openai>=2.0.0              # OpenAI API (embeddings + extraction/merge)

# Multi-LLM Support (Anthropic Claude)
anthropic>=0.70.0          # Anthropic Claude API for extraction/merge

# CORS support
fastapi-cors==0.0.6

# Authentication dependencies
bcrypt>=4.1.0              # Password/API key hashing
PyJWT>=2.8.0               # JSON Web Token encoding/decoding
cryptography>=41.0.0       # Required by PyJWT for RS256 (optional)
email-validator>=2.0.0     # Required by Pydantic EmailStr

# Development dependencies
pytest==8.3.4
pytest-asyncio==0.24.0
black==24.10.0
